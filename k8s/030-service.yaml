apiVersion: v1
kind: Service
metadata:
  name: openclaw
  namespace: claw-code-local
spec:
  type: ClusterIP
  ports:
    - port: 18789
      targetPort: 18789
      name: web
    - port: 18790
      targetPort: 18790
      name: bridge
  selector:
    app: openclaw
