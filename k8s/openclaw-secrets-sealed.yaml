# Placeholder for sealed secrets — committed empty so the ArgoCD app has a
# manifest to sync. The deploy.yml workflow replaces this file on every
# sealed-secret rotation run with the real SealedSecret content.
# Do not edit manually; changes will be overwritten.
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: openclaw-secrets
  namespace: openclaw
spec:
  encryptedData: {}